# Findings log

Things noticed while working on a task that are **out of scope for that task**.
Do not fix them in place — record them here and continue. See `AGENTS.md` §3.

| # | Source | Location | Observation | Status |
| --- | --- | --- | --- | --- |
| F1 | spike | `src/extract/extract.ts` | **HTML entities are not decoded.** Extracted title reads `Best Woocommerce Plugins &amp; WordPress Addons`. Affects quote fidelity, so it bears directly on the SC-110 verification gate — a quote containing `&amp;` will not match what a reader sees. Needs an entity decoder in the normalisation path. | RESOLVED — entity decoder added in `src/extract/entities.ts` |
| F2 | spike | `src/semantic/state.ts` | **154 decision calls for one page.** acowebs.com has 150 headings, many of which head sections with 0–2 passages. Tiny sections should be merged with their neighbour before state building, and a hard section cap applied. Matters because it may breach the 50-external-subrequest limit. | RESOLVED — section selection cut 154 calls to 38 |
| F3 | spike | `wrangler.jsonc` | **Extraction costs ~0.02 ms/KB**, so 10 ms CPU ≈ 500 KB of HTML. The planned 2 MB cap would return error 1101 on the free tier. `MAX_PAGE_BYTES` lowered 2 MB → 500 KB. Larger pages need Workers Paid. | RESOLVED in spike |
| F4 | spike | `wrangler.jsonc` | **The `ai` binding forces a remote proxy session**, which needs Cloudflare auth — breaking offline testing (AGENTS.md §8). Split out `wrangler.test.jsonc` with no `ai` binding; tests use the `replay` backend. | RESOLVED in spike |
| F6 | live Jev call | `src/provider/validate.ts` | **`noul` answers carry no `confidence` field** (verified against Jev 1.13.0). The schema required one, so every yes/no answer would have been rejected as malformed. Confidence is now derived as \|value − 0.5\| × 2. Found only by making a real call — exactly why AGENTS.md forbids guessing the wire shape. | RESOLVED |
| F7 | live Jev call | `src/semantic/questions.ts` | **`score` is a probability-weighted index over the rubric, 0-based** — 0.48 against a five-level rubric means "between level 0 and level 1". It is NOT a percentage and must never be rendered as one (invariant 2). | NOTED — enforce in SC-112 |
| F8 | live run | `src/semantic/state.ts` | **Isolated testimonials still leak through.** Cluster detection needs a run of 3+ name-headed sections; a single review sitting alone is still analysed and produces unactionable advice. Consider schema.org `Review` markup detection as a second signal. | RESOLVED — two-pass voice + adjacency detection |
| F9 | live run | `src/ui/dashboard.ts` | The dashboard originally rendered raw decisions (`self_contained: yes`, "decision calls") instead of findings — mechanism, not meaning. Rebuilt around templated findings. The underlying lesson: **anything shown to a user must answer "what do I do", not "what did the system compute".** | RESOLVED |
| F10 | user report | `src/semantic/state.ts` | **Testimonial content still reached the report.** Root cause was threefold: (a) the username regex rejected digits, so `jodiebradshaw1974` was never matched; (b) many genuine reviews carry no first-person voice at all ("Fair price, fantastic support"), so a voice-only test missed roughly a third; (c) `selectClaimCandidates` scanned all passages globally and walked straight back into the review block. Fixed with two-pass detection — seed on reviewer voice, expand across adjacent review-shaped sections — and by applying the same exclusion to passage-level checks. | RESOLVED |
| F11 | user report | `src/ui/dashboard.ts` | **Results were unreadable:** a flat list where 13 of 19 cards said the same thing. Regrouped by issue type with counts, priority filters, and per-instance detail behind a toggle. Lesson: repetition in a findings list is a rendering problem, not a data problem. | RESOLVED |
| F12 | site scan | `src/semantic/state.ts` | **Link cards and related-post lists are treated as content sections.** A 4-page scan of sanjayshankar.me produced 12 × `answer_absent`, most of them article titles inside "related posts" blocks — a heading with a one-line teaser is not a section failing to answer itself. Same class of problem as testimonials (F10): navigational/teaser blocks need excluding before analysis. **This is the next task.** | RESOLVED — root cause was nav/footer/aside never excluded from extraction; site chrome now suppressed, plus cross-section boilerplate detection |
| F13 | site scan | `src/index.ts` | Grammar: limits block emitted "1 sections look like…". | RESOLVED |
| F14 | user report | `src/ui/dashboard.ts` | **Accordion toggles silently opened the wrong page's content.** `renderGroups` numbered its panels `g0`, `g1`… per call, so an 8-page scan produced eight elements with id `g0` and `getElementById` returned the first. Now uses a module-level counter. | RESOLVED |
| F15 | SC-106 | `src/extract/extract.ts` | Suppressing every `<header>` deleted article H1s (`<header><h1>`), producing false "no H1" findings. Narrowed to `body > header`. Caught by the new static checks within minutes of writing them. | RESOLVED |
| F16 | SC-107 | `src/ui/dashboard.ts` | **`\n` inside a regex literal resolves to a real newline** in a TS template literal, splitting the regex across lines and breaking the whole script again. Harmless inside a string, fatal inside a regex — which is why the earlier escape audit missed it. Caught by the `parses as valid JavaScript` test rather than by a user this time. | RESOLVED |
| F17 | SC-107 | `src/static/language/signals.ts` | List items rarely end in a full stop, so the sentence splitter read a whole bullet as one 71-word sentence. `long_sentences` now considers paragraphs only. | RESOLVED |
| F18 | SC-114 | `wrangler.jsonc` | **The 500 KB intake cap was too high.** Enforcing the CPU gate showed extraction is superlinear at the top: 300 KB costs 6 ms but 500 KB costs 11 ms — over the hard 10 ms limit. `MAX_PAGE_BYTES` lowered 500 KB → **300 KB**. Recording timings had hidden this; enforcing them exposed it immediately. | RESOLVED |
| F19 | SC-113 | `test/unit/linkcard.test.ts` | Tests were fetching a live site 5 times, violating AGENTS.md §8. Flaky, and would fail in CI where no network egress is assumed. Frozen as a fixture. | RESOLVED |
| F20 | user report | `src/ui/dashboard.ts` | **Single-URL analysis errored for any page containing a hyphenated hype term.** `mark()` escaped `-` when building the highlight regex, but the regex carries the `u` flag (needed for `\p{L}`) and under Unicode mode `\-` is an *invalid escape* that throws at construction. All 11 hyphenated lexicon terms — `world-class`, `cutting-edge`, `best-in-class` — crashed the whole render. `-` is only special inside a character class; this is outside one. | RESOLVED |
| F21 | copy-prompt work | `src/ui/dashboard.ts` | Adding a second `document` click listener **silently replaced the first** in the test harness, breaking filters and accordions. Folded into the existing delegated handler; a test now asserts exactly one click listener exists. | RESOLVED |
| F22 | user report | copy across all templates | Finding copy read as jargon — *"Either add the substance the heading promises, or remove the heading so it stops making a promise the page does not keep."* Rewritten in plain English across 60 strings. | RESOLVED |
| F23 | SC-114 | `test/unit/cpu.test.ts` | CPU test flaked in the full suite (9 ms) while passing alone (6 ms) — Vitest runs files in parallel and a median is inflated by contention. Now takes the **minimum** sample: the cost of the code, not the load on the machine. | RESOLVED |
| F5 | spike | local dev | Wrangler cannot pick between multiple Cloudflare accounts non-interactively. Needs `account_id` in config or `CLOUDFLARE_ACCOUNT_ID`. Document in the self-host guide (SC-115). | OPEN — assign to SC-115 |
| F20 | SC-116 baseline validation | `test/unit/cpu.test.ts:63` | Before UI edits, full suite reported acowebs extraction 12ms against 8ms budget (122 passed, 1 failed). No extraction/backend code changed in SC-116. Recheck CPU performance independently of UI work; do not raise the threshold. | OPEN |
