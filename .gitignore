# Secrets — never commit
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
!.env.example

# Dependencies / build
node_modules/
dist/
.wrangler/
.vite/

# OS / editor
.DS_Store
*.log

# Working backups of planning docs — history lives in git, not in .prev files
*.prev
*.rev[0-9]
